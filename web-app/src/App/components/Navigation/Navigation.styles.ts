import { withStyles } from '@material-ui/core';

export default withStyles({
  bottomNavigation: {
    flexShrink: 0,
  },
  container: {
    display: 'flex',
    flexDirection: 'column',
    flexGrow: 1,
    overflow: 'auto',
  },
});
