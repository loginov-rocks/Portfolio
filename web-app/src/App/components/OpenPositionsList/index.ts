import OpenPositionsList from './OpenPositionsList';
import enhance from './OpenPositionsList.enhancer';
import style from './OpenPositionsList.styles';

export default style(enhance(OpenPositionsList));
