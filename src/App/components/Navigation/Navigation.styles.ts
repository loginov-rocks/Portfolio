import { withStyles } from '@material-ui/core';

export default withStyles({
  bottomMock: {
    height: 56,
  },
  bottomNavigation: {
    bottom: 0,
    left: 0,
    position: 'fixed',
    right: 0,
  },
});
